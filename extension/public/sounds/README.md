Put the default alarm sound here as `wake_up.mp3`.

Copy it from the original prototype repo
(kartik-tiwariR20/Studytrackk-extension) — same file the Python script
already uses. Users can always override it per-device from the popup's
"Wake-up sound" card, which is stored separately in chrome.storage.local
and doesn't touch this file.
